[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "evolutionary-agent-testbed"
version = "0.1.0"
description = "Experimental testbed for evolutionary agents"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]